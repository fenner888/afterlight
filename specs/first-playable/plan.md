# First playable — plan

Only after kickoff and Mark's instruction. One tested incident with simple geometry first.

At kickoff, recheck the competition rules and approved specs, then verify package registry/repository/publication history and pin exact dependencies before installing. The kickoff text supplied during planning is a future prompt, not current implementation authorization.

1. Review the defined units, loads, capacity stages, routes, durations, action order and two hand-calculated runs in [scenario.md](scenario.md). Resolve any changes in the spec before code.
2. Build/test the deterministic domain and the single validated Storm 01 scenario.
3. Connect inspector and blockout renderer.
4. Prove crew exclusivity, dependency checks, separate repair/reconnection, disconnect/reallocation, backup expiry, 0 → 6 → 13 CU capacity and both priority outcomes. Show downtime per service and test invalid actions plus the keyboard flow.
5. Add pause/replay; compare state hashes across render schedules.
6. Show Mark the running blockout, validation results and limitations, then stop for review. Detailed art and additional features require that review; no commits or deployment without Mark's review.

Specs change before direction changes. Reference art is not implementation evidence.
